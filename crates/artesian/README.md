<!-- SPDX-License-Identifier: Apache-2.0 -->

# artesian

Open, local-first **memory control plane** for AI agents.

This crate is a name-reservation placeholder. The implementation lives in the
[aquifer-labs/artesian](https://github.com/aquifer-labs/artesian) workspace —
see the `aquifer` (memory) and `artesian-mcp` (MCP server) crates.
